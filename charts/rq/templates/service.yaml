apiVersion: v1
kind: Service
metadata:
  name: {{ include "rq.fullname" . }}
  labels:
    {{- include "rq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rq.selectorLabels" . | nindent 4 }}
